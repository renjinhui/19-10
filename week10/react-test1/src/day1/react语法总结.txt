1、只要是在react的结构中写变量  我们就用 {} ;{}中只能写表达式

2、react的组件都是一个函数 或者 类， react组件在使用时 首字母必须大写
    组件也是只能有一个根元素，这点跟VUE一致；

3、react的子元素不能是一个对象，可以是 基本数据类型 或者  数组；

4、元素的类名不能写class 必须写成className 动态类名是需要我们手动拼接在一起

5、元素的行内样式 必须是一个对象的形势

6、循环 我们使用 map；    判断 我们使用三元
